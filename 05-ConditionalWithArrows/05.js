function hoopCount (n) {
   //your code goes here   

   // First attempt:

  /* if ( n >= 10) {
    return "Great, now move on to tricks"
  } else {
    return "Keep at it until you get it"
  } */

    // Second one, using arrows.
   return n >= 10 ? "Great, now move on to tricks" : "Keep at it until you get it";
}